apiVersion: v1
kind: ConfigMap
metadata:
  name: opa-policy
  namespace: opa-system
data:
  policy.rego: |
    package istio.authz
    import input.attributes.request.http as http_request

    default allow = true